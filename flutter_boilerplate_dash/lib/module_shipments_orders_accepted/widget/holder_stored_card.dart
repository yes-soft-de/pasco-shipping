import 'package:flutter/material.dart';

class HolderStored extends StatelessWidget {
  const HolderStored({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
